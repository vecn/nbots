model {
    components {
    	nbots(NativeLibrarySpec) {
	    sources {
		c {
		    source {
			srcDirs "src"
			include "**/*.c"
			exclude "**/*cairo*"
		    }
		    exportedHeaders {
			srcDirs "include"
		    }
		}
	    }
	}
    }
}
